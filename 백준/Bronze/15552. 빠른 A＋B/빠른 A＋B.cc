#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int T,A,B= 0;


	cin >> T;
	for (int i = 0; i < T; i++) {
		int sum = 0;
		cin >> A >> B; 
		cout << A + B << "\n";
	}
	return 0;
}

